---
client_slug: real-estate-dev
display_name: "[PLACEHOLDER — operator fills in real client name]"
delivery_email: "[PLACEHOLDER — operator fills in client delivery email]"
default_template: market-intelligence
notes: First test client.
---

# Real-estate development client (template)

First non-operator test client. Replace **both** the `display_name` and
`delivery_email` placeholders above with the real client's name and email
before running a brief that gets delivered. Until then, use `--dry-run` or
the `operator` client for testing.
